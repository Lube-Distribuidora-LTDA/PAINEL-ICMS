// ⚠️ TEMPORÁRIO — ver middleware.ts. Reverter para false assim que o TI
// da Lube liberar supabase.co no Wi-Fi (é o que impede o magic link de
// sair do aparelho hoje). Enquanto true, o painel fica público.
export const AUTH_DESLIGADA_TEMPORARIAMENTE = true;
